import { fetchOpenAI } from '../src/api';

describe('API', () => {
  test('fetchOpenAI should fetch data from OpenAI API', async () => {
    // Mock the fetch function
    // Call fetchOpenAI with valid parameters
    // Check if the data is fetched correctly
  });

  test('fetchOpenAI should throw an error for invalid API key', async () => {
    // Mock the fetch function to return an error response
    // Call fetchOpenAI with an invalid API key
    // Check if the expected error is thrown
  });
});